nodeLinker: node-modules
networkConcurrency: 1
httpTimeout: 300000

# SDK 58 is still in beta, so Expo publishes patch versions inside the 24h age gate.
npmPreapprovedPackages:
  - "expo"
  - "expo-*"
  - "@expo/*"
  - "babel-preset-expo"
  - "jest-expo"
